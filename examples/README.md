# Examples

This folder contains public, safe-to-commit examples.

- `sample_project/` is a fully runnable fictional demo.
- `bioelectricity_test_case/` contains citation metadata only for SAGE-hosted
  Bioelectricity articles. It does not include full article text.

Run:

```bash
atlasx run --project examples/sample_project --provider offline
```

